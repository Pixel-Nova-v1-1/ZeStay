rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow only authenticated users to upload/download files
      allow read, write: if request.auth != null;
    }
  }
}
